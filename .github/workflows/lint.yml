# Disabled: ultracite has 10k+ errors across the codebase (inherited from template).
# Re-enable after cleaning up lint errors or switching to a lighter linter config.
# See: https://github.com/RCushmaniii/ai-chatbot-saas/actions/workflows/lint.yml
name: Lint
on:
  workflow_dispatch: # Manual trigger only — no longer runs on every push
